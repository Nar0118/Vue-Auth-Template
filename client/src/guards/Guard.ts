import router from '../router'

// const isAuthenticated: boolean = true // this var was created for test

// router.beforeEach(async (to, from) => {
//   if (!isAuthenticated && to.name !== 'Login') {
//     return { name: 'Login' }
//   }
// })
